using System;

namespace MainMeth
{
    internal class Program
    {   //Главный метод возвращает результат:
        public static int Main()
        {
            
            Console.WriteLine("Главный метод возвращает результат!");
            //Рзультат главного метода:
            return 0;
        }
    }
}